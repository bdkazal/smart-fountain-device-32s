# Phase 1 Timing Notes

Target timing copied from the tested old firmware source:

- Command poll interval: 2 seconds
- Command failure backoff: 30 seconds
- HTTP timeout: 5 seconds
- Config refresh: 120 seconds
- No-command log interval: 30 seconds

This note is temporary and will be replaced by code changes when the connector accepts the firmware file write.
